## 论文自动化润色与 LaTeX 结构保护项目 PRD

### 1. 项目背景与目标

 **目标** ：构建一个基于 Cursor 的半自动化工作流，在完整保留 LaTeX 引用（Cite/Figure/Table/Equation/Algorithm）和文件路径的前提下，完成导师意见修改、马来西亚学术规范润色及 AI Writing 降重处理。

### 2. 核心业务需求 (User Stories)

* **结构锁定** ：AI 必须识别并绕过 `\cite{...}`, `\ref{...}`, `\begin{table}...\end{table}` 等特定标签。
* **规范对齐** ：自动将 "I", "We", "My" 转换为被动语态或客观陈述；确保段落长度 **$\ge 3$** 句话。
* **降重处理** ：针对润色后的文本，在保持学术严谨性的前提下，改变句式结构以通过 AI 写作检测。
* **增量修改** ：根据导师提供的特定意见（如：Comment 1: Expand the discussion on...）进行内容扩充。

### 3. 功能模块拆解 (Functional Decomposition)

| **模块名称**               | **功能描述**               | **核心约束**                            |
| -------------------------------- | -------------------------------- | --------------------------------------------- |
| **LaTeX 语法锚点保护**     | 定义 AI 不可触碰的正则表达式模式 | 严禁修改 `{}`内的路径和 Key                 |
| **马来西亚学术风格 Agent** | 替换第一人称，检查段落连贯性     | 必须符合马来西亚 Thesis 惯例（Passive Voice） |
| **AI 痕迹消除 Agent**      | 调整词汇丰富度和句式复杂度       | 不能为了降重而损失学术专业词汇                |
| **结构完整性校验**         | 最终输出前的语法与引用完整性扫描 | 确保编译无误                                  |

---

## 🛠 技术实现路径 (Technical Implementation Path)

建议在 Cursor 中采用 **"三步走"** 的方法： **预处理声明 -> 分阶段处理 (Skills) -> 后验检查** 。

### 第一阶段：初始化环境与规则设定

在你的项目根目录下创建 `.cursorrules` 文件，这是让 Cursor 深度理解你 LaTeX 结构的“大脑”。

### 第二阶段：Agent Skills 定义

我们将任务拆解为三个核心 Skill 提示词，你可以直接在 Cursor 的 `Skills` 设置中添加，或者作为 `.md` 文件放在项目内供 Cursor 读取, 可是使用 skill-creator 帮助创建更规范的 skill。

### 第三阶段：执行流

1. **逐章节处理** ：不要一次性输入整篇论文，建议以 `Chapter` 为单位进行。
2. **上下文关联** ：在 Cursor Chat 中利用 `@filename` 引用导师的意见文档（如 PDF 或 Text）。

---

## 🤖 Cursor Agent Skills 详细配置

Agent Skills 创建时请参考.cursor下的 skills 的方式，帮我做一个符合步骤要求的skills。我建议你先看下网上怎么写agent skills，然后看下目录里已有的skills做参考，再规划如何生成skills，并一步一步实现。注意，skills里用到规则、模板、脚本，要单独生成。不是都写到skills里，也不是每次执行skills的时候再生成。也可以考虑使用 skill-creator 进行生成。

Agent Skill 的核心诉求如下：

### Skill A: LaTeX Content Guard (结构保护专家)

> **Role** : 你是一个 LaTeX 语法保护专家。
>
> **Task** : 在润色文本时，识别并完全忽略以下内容：
>
> 1. 所有以 `\` 开头的引用命令：`\cite{...}`, `\ref{...}`, `\label{...}`, `\url{...}`。
> 2. 所有文件路径：例如 `chapter_4/1.jpg`，严禁修改路径字符串。
> 3. 环境块：`\begin{table}...\end{table}`, `\begin{figure}...\end{figure}`, `\begin{equation}...\end{equation}`, `\begin{algorithm}...\end{algorithm}`。
>    **Output Constraint** : 仅对非环境块内的 **正文文本** 进行操作，保留所有 LaTeX 符号（如 `$ ... $`）。

### Skill B: Academic Polisher - Malaysia Standard (学术润色专家)

> **Role** : 马来西亚高校学术论文编辑。
>
> **Task** :
>
> 1. **去第一人称** ：将所有 "I", "We", "Our", "My" 转换为被动语态或以 "This study", "The analysis" 开头的客观描述。
> 2. **段落优化** ：检查每个段落，若段落少于 3 句话，必须结合上下文进行逻辑扩充或合并。
> 3. **用词专业化** ：使用更高级的学术动词（例如用 "investigate" 代替 "look into"）。
> 4. **符合标准** ：遵循马来西亚（如 UM, UPM, UTM）常见的论文格式规范。

### Skill C: Human-Like Rewriter (降重与去 AI 化)

> **Role** : AI 写作降重专家。
>
> **Task** :
>
> 1. **句式变换** ：在不改变学术原意的前提下，交替使用长短句，增加 Burstiness（突发性）。
> 2. **同义替换** ：替换过度频繁使用的连接词（如 Replace "In addition" with "Furthermore" or "Moreover" intermittently）。
> 3. **困惑度增强** ：微调语序，避开典型的 GPT 写作风格（如“Firstly, Secondly, Finally”这种死板结构）。

---

## 📝 实际操作指令示例 (Prompt to Cursor)

当你准备开始修改 `Chapter_4.tex` 时，可以在 Cursor Chat 中输入：

> `@Chapter_4.tex` `@Supervisor_Comments.pdf`
>
> 请结合导师意见，调用 **Academic Polisher** 和 **LaTeX Content Guard** 技能对本章进行修改。
>
> **特别要求** ：
>
> 1. 确保所有 `\cite` 和图片路径 `chapter_4/xxx.jpg` 保持原样。
> 2. 修正文中提到的第一人称。
> 3. 针对导论部分（Section 4.1），按照导师要求增加关于 "Data Reliability" 的讨论，确保该段落至少有 5 句话。
> 4. 最后运行 **Human-Like Rewriter** 降低 AI 检出率。
> 5. 输出完整可用的 LaTeX 代码。
>
